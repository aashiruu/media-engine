apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: media-engine-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: media-engine
  namespaceSelector:
    matchNames:
      - default
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
